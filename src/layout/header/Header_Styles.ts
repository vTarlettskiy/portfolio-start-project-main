import styled from "styled-components";

const Header = styled.header`
    background-color: transparent;
    padding: 16px 0;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 9999999;
`

export const S = {
    Header
}